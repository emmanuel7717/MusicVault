## Team Contributions

- **Emmanuel:**  
  - Designed the data structure and created `songs.json` with sample data.  
  - Implemented the **song model** with CRUD operations.  
  - Set up **server.js** with app-level middlewares (JSON parsing, 404 handler, error handler).  
  - Tested all routes on Postman to ensure functionality.  

- **Divy:**  
  - Created the **routes** for songs using Express Router.  
  - Implemented **validation middleware** for POST and PUT requests using `express-validator`.  
  - Helped organize the **modular folder structure** (`models`, `routes`, `middlewares`).  
  - Assisted with debugging and ensuring proper HTTP status codes.
