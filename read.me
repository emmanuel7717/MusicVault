WORK SPLIT

Emmanuel:

Made the data file songs.json and added sample songs.

Built the model with all CRUD functions (get, add, update, delete).

Set up the main server with middlewares (JSON parsing, 404 + error handling).

Tested all routes in Postman to make sure everything worked right.

Divy:

Built the routes for songs using Express Router.

Set up validation middleware for POST and PUT using express-validator.

Helped build the clean modular folder setup (models, routes, middlewares).

Helped fix bugs and made sure status codes and responses were correct.